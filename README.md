# 藥學國考隨機藥物分類卡

本資料夾可直接部署至 GitHub Pages。

## 部署
1. 建立 GitHub repository。
2. 將 `index.html`、`.nojekyll` 與本檔案上傳至 repository 根目錄。
3. 到 Settings → Pages。
4. Source 選擇 Deploy from a branch。
5. 選擇 `main` 與 `/(root)`，儲存後等待部署完成。

首頁檔案必須保留名稱 `index.html`。
